# Changelog

## Unreleased (Waves 15-29 + UI S1-S4)
- Waves 27-29 + UI S1-S4 (2026-09-23): DDS multi-mip BC1-5 (Slice A BC1-3 all-mips + Slice B BC4/BC5-UNORM, SNORM/uncompressed explicit) + renderer Slice C (`setTextureMips` authored-primary, TANGENT wire-only byte-identical, `drawMeshRange` solid/textured utilities, aniso 4x; C2 deferred: 10 range variants, per-submesh SRV routing, caches, async) + offscreen viewport test + GPU tangent pin + `cli-orient-two-bone/gr22smd-missing/fbx2smd-missing`; quaternion sampling (parallel `quat` track, shared `sampleBone`, binary search; wrap-parity branch-cut lesson fixed via protocol) + compression (key-reduction, smallest-three-14, pos-range-16, `.ani` stays export-only with bytes pinned); bridge closeout (per-call log, extractor hygiene, fail-closed imports, probe-before-stage) + FBX indexed dedup + workspace restore round-trip asserts + glTF 29a import (cgltf `85cd6238`, 2 root-cause fixes via protocol: matrix transpose `R=M^T` + PBR factor copy; export 29b deferred); UI honesty gates + toolbar Undo/Redo/Validate + menu parity + keyboard gate + cheatsheet + dead sweep; 5 new skills + 4 agents. 162/162 + 15/15 green release AND debug.
- Wave 27 Slice A (BC1-3 multi-mip decode, 2026-09-22): `decodeDds` decodes ALL declared BC1-3 mip levels (halving floor/min-1, per-level length validation, truncated tail is an explicit `dds.decode` failure); `DdsImage::mips` holds every level and legacy `rgba` stays a byte-identical mirror of `mips[0].rgba`; shared `decodeBcLevel` helper removes the base-only duplicate path (DXT1 1-bit-alpha, DXT3 nibble, DXT5 ramp behavior unchanged); regression `dds.decodes_dxt1_8x8_two_mips` (8x8 red + 4x4 green: level sizes + pixel values + mirror) and `dds.truncated_mip2_fails` (missing/partial mip1 tail fails). Deliberately deferred to Slice B: BC4/BC5 single/dual-channel decode + uncompressed RGBA (still explicit failures); Slice C: renderer authored-mip upload + per-submesh binding (renderer.cpp/shaders untouched).
- Wave 22 (coordinate-system hardening + transform editing, 2026-09-18): new `coordsys` module (canonical Y-up internal system, per-format conversion profiles for FBX/GR2/SMD/Noesis, `tryConvertToCanonical` fails explicitly on unimplemented spaces instead of silent identity); FBX reader + GR2/grnreader98 import routed through the profiles (inline matrix copies removed); ROOT-CAUSE orientation fix — bone/frame rotation conjugation was `M*R*M^-1`, must be `M^-1*R*M` (basis-change direction proven by `(u*M)*R'=(u*R)*M`; invisible for identity binds, detached mesh from skeleton under posing for rotated binds); ROOT-CAUSE hierarchy fix — globals composed `parent * local` instead of `local * parent` for the row-vector convention (applied child rotation to parent translation; correct only for identity binds, scrambled every rotated-bind skeleton: 243-unit detachment on warrior_m.gr2); fixed in `skeleton.cpp` + gizmo decomposition + test mirror, exports unaffected (locals untouched); automated transform diagnostics (`diagnoseOrientation` + `m2rig_cli orient`, exit 0/3: head/feet order, mesh overlap, scale advisory, rigid-bind distance); new headless `m2rig_cli gr22smd` (GUI-parity GR2 path); real-data proof — warrior_m.gr2, sura_m.gr2, warriorout.fbx and Noesis-experiment FBX all ORIENTATION SANE after the fix (worst rigid 27-35 on ~175-tall models); build-break repairs (duplicate `computeNormals` in `mesh.cpp`, `GpuVertex` 72 B assert + D3D11 input-layout COLOR@48/TEXCOORD@64 fix — stale 48 B layout corrupted colors/UVs, renderer texture-API header/impl drift reconciled with real `GenerateMips` + `hasTexture` + `setActiveTexture` decl, unimplemented PBR/IBL method decls removed); Bone panel numeric Position/Rotation(deg)/Scale editing (undoable, lock-aware, finite/near-zero guards) + Frame bone + Select mirror + Copy/Paste + Reset-to-bind; gizmo World/Local orientation modes; viewport Back/Right/Bottom presets; FBX model-set test dataset-aware (9-file checkout no longer false-fails); 14 new tests (100/100 + 8/8 green, debug + release).
- Wave 26 + UI unification (2026-09-22): deterministic KD-tree core (`spatial.hpp/cpp`: median split on longest axis with x>y>z priority, total (coord,index) order, exact top-k/radius matching brute force up to 1-ulp boundary ties, NaN-safe ordering + never-insert) replacing brute force in `transferWeightsKDTree`, `buildDonorTable` and `findMirrorPairs` (blend/greedy code untouched; `autoRigMesh` deliberately brute-force — segment metric is point-incompatible, proven by counterexample); UI: toolbar "Auto-rig" (same undoable op, red severity family, disabled without asset), Flood/Prune red + tooltips, viewport Shading popover (7 modes + overlays + Paint guard), input guard suppressing orbit/pan/paint/zoom/click-select while the popover is open, teal-cyan theme, Quick/Self-train tooltips, Locked label lists all ops, USER_GUIDE truth (steps, Shading/PBR/Auto-rig, Quick-vs-Selftrain, GPU-skinning limits); 15-agent audit swarm, all findings incorporated (NaN UB fix, signature fix, input guard, tooltip/label/guide fixes). 137/137 + 12/12 green release; debug 137/137 green.
- Wave 25b (IBL: irradiance SH + prefiltered env + BRDF LUT, 2026-09-21): procedural sky (analytic, no assets) + CPU `ibl.hpp/ibl.cpp` (evalSky, SH project with cosine-lobe band scales, shIrradiance, half codec — unit-pinned: uniform sky yields exactly L*pi, determinism, sky shape, codec bits/roundtrip) + init-time GPU bake (CPU-filled 128 base cube, 64-tap GGX prefilter mips 1-7, 128 BRDF LUT, 9-coeff irradiance CB b3, wrap/clamp samplers) + `PbrLighting` IBL terms (world-space via inverse view rotation, split-sum specular, ambient placeholder deleted outright); `CubeDir` table derived from view math and proven against hardware sampling by the mirror-sun case. ROOT-CAUSE debugging showcase: metallic specIBL read exactly 0 while dielectric matched — staging readback proved prefilter mips black while base/BRDF were perfect; cause: D3D11 force-unbinds an SRV whose RESOURCE is bound as RTV (even disjoint subresources), so single-cube sampling read black. Fixed with source/destination cube split (+base copy); all reference pairs now agree within 1 LSB (metallic 107, mirror-sun 255). Wave-local failures fixed via protocol (setTexture signature corruption by a mega-insert — caught by audit pre-build; HLSL fwd-decl order; BRDF LUT V-flip caught by audit pre-run; joint-vs-separable kept as documented consistent pair). 132/132 + 12/12 green release; debug 132/132 green with identical pixels.
- Wave 25a (PBR punctual backend, 2026-09-21): Cook-Torrance GGX (`D_Ggx` + Schlick-GGX `G` + Schlick Fresnel, metalness workflow, punctual + ambient placeholder — IBL is 25b) + `PbrMat` factor CB (b2, PS-only) + `setPbrMaterial` + `drawMesh[Textured/Skinned/TexturedSkinned]Pbr` (Blinn fallback discipline, PS restore); per-asset `LoadedAsset::pbr` + `App::usePbr` (Solid/SolidWireframe only, debug stays flat, Wireframe stays Blinn) + Materials sliders (metallic/roughness/AO, CB-per-draw so no gpuDirty) + toolbar checkbox + View menu item; `computeTangents` wired into all mesh producers (SMD/FBX preserve file normals — no silent recompute; LOD/samples recompute-then-tangents) + `computeNormals` dead flag param removed (was ignored; all callers wanted recompute). Tests: `render.pbr_punctual_values` (9 hand-verified pixel sets: metal 98,98,104; dielectric 118,118,124; sharp 255 vs broad 151 on H-aligned geometry after a misdesigned-geometry failure taught that off-lobe roughness is correctly ~117; greybase 86; AO 107<118; emissive 215; textured 58; skinned twins identical) + `core.tangents_*` (sample orthogonality/handedness, degenerate-UV (1,0,0,1) fallback, normal preservation). Data proof on 31 user-supplied GR2: 31/31 convert, 30/31 SANE (41-102 joints incl. 90-bone ninja, diacritics `ninjałka.gr2` converts cleanly end to end); Gryphon honestly CHECKs (static 1-node prop, gate working as designed — static-prop advisory rule is follow-up). Audit GO-WITH-NOTES, all actionable notes incorporated (tangent order verified, CB mapping verified, flag removed). 128/128 + 12/12 green release; debug 128/128 green.
- Wave 24 (GPU skinning LBS, 2026-09-21): `SkinVertex` stream (32 B, second VB slot) + `VsSkinned` + 256-matrix palette CB (b1, identity-padded) + `uploadSkinning/hasSkinning/releaseSkinning/setSkinningPalette` + `drawMesh[Textured/Flat/WireOverlay]Skinned` (layout/VS/PS restore discipline, vertex-count pairing guard, stale-skin drop on re-upload); `refreshGpu` uploads bind + skin once (legacy CPU-DQS path preserved: deformed verts, never paired with a stream — double-deform proven impossible); `drawSceneContents` sets the per-frame `bindInverse * currentGlobal` palette (identity at bind, one uniform path); `buildSkinVertices` fails explicitly on dangling/oversize ids, >4 influences, bad weights. Tests: `core.skinning_stream_*` (sample armor, rejects, unweighted-zeros) + `render.gpu_skinning_lbs` (identity bit-exact diff 0, translate moves 3072 B, textured 78, flat 255,0,0, overlay 95 px, sample-armor 1036 px). Audit NO-GO items fixed pre-build (SkinVertex fwd decl, test include, parity diction narrowed to accepted inputs, slot-1 unbind). RHI interface deliberately deferred (one backend + no second consumer = dead abstraction; lands with the null backend). 125/125 + 12/12 green release; debug 125/125 green with identical pixels.
- Steps 3-4 (bridge hygiene + triage flag, 2026-09-21): unique Noesis tmp per call (PID + atomic counter + stem framing in `importBridgedFile`; async `runBridgeChain` already had PID + counter) — shared `m2rig_bridge_import.smd` race killed, stale-file installs impossible; best-effort tmp remove on both Noesis `timedOut` paths; FBX export tmp + `exportGr2ViaBridge` tmp uniquified (keepTempSmd inspection contract preserved: deterministic name only when keeping); missing cleanup added on export bridge failure paths; `-a -t` semantics documented as observed-but-unverified (orient gate is the safety net); `extractWeightsFromGr2ViaBridge` sharpened to explicit-unsupported boundary probe; CLI header synced to all 16 verbs (1:1:1 header-usage-dispatch); USER_GUIDE truth (CPU LBS + DQS toggle, MSE parse/CLI wired vs overlay roadmap, `.ani` export-only); `M2RIG_HEAP_TRIAGE` option (default OFF) gates the debug CRT heap validation — debug suite fast path proven: 122/122 green in ~124 s wall without triage (was >30 min); 3 new honest fixture suites `cli-learn-from-asset/self-learn-autorig/self-learn-transfer` (exit 0 verified; gr2-dir/analyze + fbx2smd unwired with stated reasons). Audit findings all incorporated (timeout litter, FBX race, `-a` header drift). 122/122 + 12/12 green release; debug m2rig_tests 122/122 green.
- Step 2 (sRGB albedo path, 2026-09-21): linear-space lighting — `PsMain`/`PsTexSrgb`/`PsTexLinear` light in linear with sRGB output encode (exact piecewise curves in `kShaderSrc`); albedo decode lives in-shader (`PsTexSrgb`, per-texture `srgb` upload flag + `textureIsSrgb` query) because explicit UNORM_SRGB views fail creation on the WARP test runtime (proven, both mipmapped and single-level — HW decode deferred to the PBR wave pending hardware proof); new `Renderer::drawMeshFlat` (unlit passthrough) + `drawSceneContents` routes Normals/Height/Weights/UV debug ramps through it so they stay exact and legend-consistent; mip tails filter in gamma space (accepted, documented); test `texture_srgb_view_and_flat_identity` pins flag lifecycle + `generateMips=false` branch + hand-computed pixels (flat 255,0,0; sRGB-128 78,78,80; linear-128 116,116,119 — old pipeline gave identical values for both, so the contrast is the true decode pin); wave-local failures fixed via protocol (HLSL `linear` keyword collision, `Impl::TextureEntry` qualification). 122/122 + 9/9 green, release.
- Wave 21 (viewport/render/session, from code evidence): offscreen viewport render target composited via `ImGui::Image()` (docking/DPI-proof scene), toast notification queue, viewport pixel-proof readback, X-ray bones default ON, DQS (dual-quaternion) deform preview path, `GpuVertex` tangent/bitangent channels (72 B) + `computeTangents`, PBR material data model (header), MSE parser module, status bar + toasts overlay.
- Wave 20: keyframed animation (`AnimClip`: capture/add/delete, shortest-path euler lerp sampling, bake to SMD frames; clip-aware timeline length/scrub/playback; keyframe row with tooltips reachable on static assets; in-session only, bake replaces imported frames explicitly); 5 tests (86/86 + 8/8).
- Wave 19: LOD decimation generator (`decimateMesh`: deterministic shortest-edge collapse, influence union + repair, submesh rebuild, bounds/normals; export-only CLI `lod [--ratio]` + GUI `Export LOD SMD...` with keep-ratio slider; 5 core tests + `cli-lod` suite; ninja 3208 -> 1603 tris, 0 errors, 0.14 s).
- Wave 18 (viewport full integration, 8-agent audit): always-drawn gizmo (`updateBoneGizmo`, no hover gate), Rotate row-strip fix, root-bone draw+pick, pick OOB guard, camera NaN sanitizing, intake parity (sample/switch/Noesis-id/async status), workspace restore with source reimport + honest failure, undo/paint honesty (history clear, zero-dab rollback, locked flood/prune fail), influence-histogram + weight-quality caches, system-scan TTL, timeline jump fix, overlay/tooltip/cheatsheet honesty; 12 new tests (76/76 + 7/7).
- Wave 17: edge-hash mesh topology in core (`buildMeshTopology`: boundary/manifold/non-manifold edge census + isolated/open verts, deterministic); validation rules `MESH_TOPOLOGY` (info), `MESH_NON_MANIFOLD` + `MESH_ISOLATED_VERTS` (non-blocking warnings, GUI + CLI); 5 core regression tests; real ninja mesh clean (3208 tris, 0 non-manifold, 0 isolated).
- Wave 16: headless `m2rig_cli autorig <in.smd> <out.smd>` (deterministic auto-rig + repair + export gate + stats; 7th ctest suite `cli-autorig` over `tests/data/two_bone.smd`); restored root `.gitignore` so `build/`, game binaries and bridge tools stay untracked; user-guide truth pass (legacy web AI/neural sections removed, real transfer + CLI documented, limitations corrected).
- Wave 15 fix pass:
- Viewport empty root cause: RH `lookAt` (view z negative) was paired with an LH projection, so every NDC z landed at ~1.007 (outside [0,1]) and D3D clipped grid + mesh + bones entirely. Projection fixed to RH-for-D3D (`m22=far/(near-far)`, `m23=-1`, ortho `1/(near-far)`); framed sample center now projects inside NDC (guard `camera_frames_sample_in_ndc`).
- Renderer: Frame constant buffer fixed to 128 B with cached lighting half (every Map rewrites all 32 floats); view-dependent lighting stable across solid/wire/overlay/textured/lines paths; `GpuVertex` 48 B layout locked by static_assert; solid rasterizer set to CULL_NONE (mixed bridge winding no longer culls the model invisible).
- Viewport/gizmo/camera: client-relative framebuffer rect (minus main-viewport origin, per-axis DPI), invalid-rect warning + live diagnostics overlay (rect/eye/dist/grid/bones), adaptive camera clip/zoom (`framedRadius` + `updateClip`, far covers model+grid), adaptive grid (`r*1.5`) + screen-aware joint crosses, empty-viewport guide + default-open skeleton root, lock-checked gizmo undo (no fake success), `inverseGeneral` world->local with scale-strip for Rotate, deformed Height parity, Material edit refreshes GPU, `F`/pan/zoom input gating, `T` shortcut hint in View menu.
- Tests: 57/57 + 6/6 green (debug + release) incl. `camera_frames_big_aabb_radius100_in_ndc` (radius-100 AABB stays in NDC + clip invariant).
- Viewport black-out root causes fixed: `PassthruCentralNode` on the dock-builder node (opaque central bg hid D3D incl. grid), `SetItemAllowOverlap` after the fullscreen `InvisibleButton` (overlay Frame/Persp/Front/Top/Left never got clicks), backbuffer readback helper + pixel assertion in the render smoke test (`873 non-clear of 4096` proves draws emit pixels).
- GUI clarity: toolbar tooltips (Grid/Bones/X-ray/Wire/Tex/Paint), Bone panel "Select root" helper + viewport hint, empty-viewport guide; `AGENTS.md` created (was missing, every agent flagged it); `scripts/package-windows.ps1` portable zip + SHA256.
- Build repair: `SetNextItemAllowOverlap` before the fullscreen `InvisibleButton` (the post-fix `SetItemAllowOverlap` does not exist in pinned ImGui and broke the GUI link, deleting the exe); `DockBuilderAddNode` reverted to the documented single-flag form (public vs internal enum mismatch C5054). Release + debug `Metin2RiggingStudio.exe` verified by timestamp + embedded new strings.
- Tests: 55/55 + 6/6 green (debug + release) incl. `deformed_height_matches_solid_at_bind` + `autorig_binds_every_vertex_deterministically`; CLI `validate-msm` runs the GUI Inspector gate; CMake duplicate link block removed.
- Skills/docs: viewport/weight-painter/regression updated; new `gizmo-manipulators/autosave-recovery/msm-inspector/textured-viewport/flood-prune-autorig`; CI workflow restored (debug + release configure/build/ctest).
- Packaging repair: restored `CMakePresets.json` so documented debug/release configure commands work again; package script now rejects unknown presets and uses `docs/USER_GUIDE.md` for its README copy. Release ZIP verified with SHA256; bridge assets remain intentionally excluded.
- Viewport visibility hardening: D3D11 scene passes now clamp panel rectangles to the actual swap-chain backbuffer and enable a matching scissor rectangle. This protects model, grid, skeleton and gizmo composition during DPI changes, resize transitions and partially visible dock layouts. Render smoke coverage now exercises an out-of-bounds panel rect.
- Runtime/UI hardening: invalid scene passes now keep a cleared RTV bound for ImGui, failed resizes remain pending and are logged, asset switching forces a real GPU upload, hidden-all submeshes release stale geometry, and the model-only render assertion no longer passes from grid pixels. ImGui layout now persists in `config/imgui.ini`; global shortcuts respect keyboard capture. Texture lookup searches executable/current ancestor roots so F5 and double-click launches find project `Data/Models` assets.

## 0.10.0 (2026-09-13)
- Textured viewport: dependency-free DDS decoder (DXT1/3/5, verified on a
  real 512x512 game texture), UV vertex channel, textured pixel shader +
  sampler, Tex toggle (toolbar, View menu, T key), missing-texture
  fallback to solid shading.
- Flood/Prune group ops over the selected bone (undoable, lock-guarded,
  mass-reported) with Bone-panel buttons.
- Async bridge imports (`startBridgedImport` + per-frame `pollBridgeImport`,
  UI stays live, import buttons gated, running-time indicator); shared
  `runBridgeChain` worker for grnreader/Noesis.
- Viewport visibility fix (NoBackground), Scale gizmo, heatmap refresh on
  selection, headless D3D11 smoke test.
- CWD anchoring (`executableDir` + `toolSearchRoots`); export-gate text
  corrected; dead API removed (mirrorXYZ no-ops, extractSourceSkin,
  writeMsmFile, exportGr2WithSmdFallback).
- Tests 53/53 + 6/6 suites green (`/W4 /WX`, release).

## 0.9.0 (2026-09-13)
- Waves 1-10 complete: core, D3D11 viewport (7 modes, X-ray, wire overlay,
  UV, heatmap), ImGuizmo manipulators, CPU skinning preview + timeline
  transport, SMD/MSM native pipeline, grnreader98 + Noesis bridges,
  weight paint/transfer/self-train/symmetry/locks, batch export, submesh
  isolation, `.m2rig` workspaces, headless `m2rig_cli`, CI.
- 41/41 checks + 5 CLI suites green (`/W4 /WX`, debug + release).
- 14 race/gender skeleton profiles; real-data verified (90-bone ninja,
  37-bone warrior).
- Skills refreshed to native reality; `DEPENDENCIES.md` added.

## 0.1.0
- Initial native project skeleton (CMake, core lib, ImGui shell).
